# Copy to .env.local and adjust as needed.
# The default points directly at the FastAPI server (works for local dev).
# If you want to use Vite's dev proxy instead, set this to /api
VITE_API_URL=http://localhost:8000
